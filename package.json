{
  "name": "cy-agent",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Headless agent runtime monorepo",
  "scripts": {
    "build": "pnpm -r build",
    "test": "vitest run",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "@types/node": "^24",
    "typescript": "^5.5.4",
    "vitest": "^4.1.10"
  },
  "packageManager": "pnpm@11.20.0",
  "engines": {
    "node": ">=24"
  }
}
