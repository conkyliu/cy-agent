{
  "name": "@cy-agent/openai-provider",
  "version": "0.0.0",
  "type": "module",
  "description": "OpenAI-compatible streaming provider (chat completions, SSE, tool calls)",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@cy-agent/agent": "workspace:*",
    "@cy-agent/protocol": "workspace:*"
  }
}
